name: V2A
options:
  bundleIdPrefix: com.charlesgxy
  deploymentTarget:
    iOS: "17.0"
  developmentLanguage: zh-Hans
  createIntermediateGroups: true
  knownRegions:
    - zh-Hans
    - en
settings:
  base:
    DEVELOPMENT_TEAM: SRYLV3Q454
    CODE_SIGN_STYLE: Automatic
    SWIFT_VERSION: "5.10"
targets:
  V2A:
    type: application
    platform: iOS
    sources:
      - path: V2A
        excludes:
          - "Info.plist"
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.charlesgxy.v2a
        DEVELOPMENT_TEAM: SRYLV3Q454
        TARGETED_DEVICE_FAMILY: "1"
        ENABLE_PREVIEWS: "YES"
        ASSETCATALOG_COMPILER_APPICON_NAME: AppIcon
        ASSETCATALOG_COMPILER_GLOBAL_ACCENT_COLOR_NAME: accent
        INFOPLIST_FILE: V2A/Info.plist
        CODE_SIGN_IDENTITY: "Apple Development"
        PRODUCT_NAME: V2A
